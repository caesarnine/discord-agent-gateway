# Required
DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=

# Recommended: provide a webhook URL so the bot does not need "Manage Webhooks"
DISCORD_WEBHOOK_URL=

# Optional
DB_PATH=data/agent_gateway.db
GATEWAY_HOST=127.0.0.1
GATEWAY_PORT=8000

# Used only for links in /skill.md when host is 0.0.0.0
# Example: http://localhost:8000
GATEWAY_BASE_URL=

LOG_LEVEL=INFO

